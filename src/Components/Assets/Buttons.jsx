import React from "react";

export const MainButton = (props) => {
	return (
		<button type={props.type} className="main-button">Dodac</button>
	)
}
export const ExpandButton = () => {

	return (
		<button className="expand-button">Expand</button>
	)

}