// The board where will be showing the features components
import React from "react";
// Components to showing

const Showcase = () => {
	return (
		<main className="showcase">

		</main>
	)
}

export default Showcase;
